# demos/

Drop the voice-agent call recordings here so the Work page audio players work:

- `voice-dental.mp3` — the dental clinic AI receptionist call
- `voice-restaurant.mp3` — the restaurant AI reservation call

Until these files are added, the audio players on `work.html` simply show no audio
(the transcripts still display, so nothing looks broken).
